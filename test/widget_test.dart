// Stage 1 widget tests — auth flow is tested via integration tests
// once real Supabase credentials are configured.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — auth integration tests go here', () {
    expect(true, isTrue);
  });
}
